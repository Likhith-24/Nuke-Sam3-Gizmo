# H2 SamViT Gizmo — SAM 2 / SAM 3 + ViTMatte for Nuke
# Requirements file for pip installation
#
# NOTE: install.py handles the full setup automatically.
# This file is provided for reference and manual installs.

# ── Build system ──────────────────────────────────────────
# SAM3 uses pkg_resources at runtime; setuptools ≥71 removed it.
setuptools<71

# ── Core ML ───────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── SAM 2 ─────────────────────────────────────────────────
# Install with:  pip install --no-build-isolation sam2
sam2>=1.0.0
hydra-core>=1.3.0
iopath>=0.1.10

# ── SAM 3 ─────────────────────────────────────────────────
# Install from GitHub (not on PyPI):
#   pip install --no-build-isolation 'git+https://github.com/facebookresearch/sam3.git'
# sam3>=0.1.0   # informational only
einops>=0.8.0
decord>=0.6.0
pycocotools>=2.0.0
timm>=1.0.0
ftfy>=6.1.0

# ── ViTMatte (alpha refinement) ───────────────────────────
transformers>=4.30.0

# ── Text prompt mode (Grounding DINO) ────────────────────
# Auto-downloaded from HuggingFace on first use; cached locally
# in models/grounding_dino/ for subsequent offline runs.

# ── Image processing ─────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.10.0
Pillow>=9.5.0

# ── System / cache ───────────────────────────────────────
psutil>=5.9.0

# ── Optional: CUDA support ───────────────────────────────
# Install PyTorch with CUDA from https://pytorch.org/get-started/locally/
# Example:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
